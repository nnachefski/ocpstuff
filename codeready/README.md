##### # 
```
cd ~
wget http://satellite.home.nicknach.net/pub/codeready-workspaces-1.0.1.GA-operator-installer.tar.gz
tar -xzvf codeready-workspaces-1.0.1.GA-operator-installer.tar.gz
cd codeready-workspaces-apb-installer/
./deploy.sh --deploy -p=ide
```
